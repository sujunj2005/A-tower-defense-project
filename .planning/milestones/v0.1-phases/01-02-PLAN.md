---
phase: 01-map-system
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/bean/session_data.gd
  - scripts/bean/option_data.gd
autonomous: true
requirements: [MAP-01, MAP-03]

must_haves:
  truths:
    - "GameSessionData stores current_map_id for battle map selection"
    - "GameSessionData stores global_difficulty preset selection"
    - "GameSessionData stores accumulated map weights from event options"
    - "GameSessionData stores pending battle modifiers from event options"
    - "OptionData carries map_weights for weighted map selection"
    - "OptionData carries force_map_id for forced map selection"
    - "OptionData carries battle_modifiers for battle content modification"
  artifacts:
    - path: "scripts/bean/session_data.gd"
      provides: "Extended session data with map selection and difficulty fields"
      contains: "current_map_id"
    - path: "scripts/bean/option_data.gd"
      provides: "Extended option data with map weights and battle modifiers"
      contains: "map_weights"
  key_links:
    - from: "scripts/bean/option_data.gd"
      to: "scripts/bean/session_data.gd"
      via: "EventSystem transfers option data to session"
      pattern: "map_weights"
---

<objective>
扩展 GameSessionData 和 OptionData 数据模型，新增地图选择和难度相关的字段。

Purpose: 为事件驱动的地图选择机制和难度系统提供数据载体。
Output: 增强的 SessionData 和 OptionData，支持地图权重、强制地图、战斗修改器和全局难度。
</objective>

<execution_context>
@E:/Test_MCP/first/TestMCP/.trae/get-shit-done/workflows/execute-plan.md
@E:/Test_MCP/first/TestMCP/.trae/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-map-system/01-CONTEXT.md
@.planning/phases/01-map-system/01-RESEARCH.md
@scripts/bean/session_data.gd
@scripts/bean/option_data.gd
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add map and difficulty fields to GameSessionData</name>
  <files>scripts/bean/session_data.gd</files>
  <read_first>scripts/bean/session_data.gd</read_first>
  <action>
在 GameSessionData 中新增以下字段（放在 current_battle_deadly 之后）：

1. `@export var current_map_id: String = ""` — 当前战斗的地图ID，空字符串时使用默认地图
2. `@export var global_difficulty: String = "normal"` — 全局难度预设，可选值：easy/normal/hard
3. `var accumulated_map_weights: Dictionary = {}` — 累积的地图权重 {map_id: weight}，非持久化
4. `var pending_battle_modifiers: Array[Dictionary] = []` — 待应用的战斗修改器，非持久化

注意：accumulated_map_weights 和 pending_battle_modifiers 不需要 @export，因为它们是运行时临时数据，每次战斗触发后清空。current_map_id 和 global_difficulty 需要 @export 因为存档系统需要持久化它们。
  </action>
  <verify>session_data.gd 包含所有 4 个新字段</verify>
  <acceptance_criteria>
    - session_data.gd 包含 "current_map_id: String"
    - session_data.gd 包含 "global_difficulty: String"
    - session_data.gd 包含 "accumulated_map_weights: Dictionary"
    - session_data.gd 包含 "pending_battle_modifiers: Array[Dictionary]"
  </acceptance_criteria>
  <done>GameSessionData 新增地图选择和难度相关字段</done>
</task>

<task type="auto">
  <name>Task 2: Add map_weights, force_map_id, battle_modifiers to OptionData</name>
  <files>scripts/bean/option_data.gd</files>
  <read_first>scripts/bean/option_data.gd</read_first>
  <action>
在 OptionData 中新增以下字段（放在 battle_trigger 之后）：

1. `@export var map_weights: Dictionary = {}` — 地图权重配置 {map_id: weight}，权重值 0.0~1.0，所有值之和应 ≤1.0
2. `@export var force_map_id: String = ""` — 强制指定地图ID，非空时覆盖权重选择
3. `@export var battle_modifiers: Array[Dictionary] = []` — 战斗修改器列表，每个 Dictionary 包含 type 和 value 字段

修改 from_dict() 方法，从字典中读取这 3 个新字段：
- `map_weights = data.get("map_weights", {})`
- `force_map_id = data.get("force_map_id", "")`
- `battle_modifiers = data.get("battle_modifiers", [])`

修改 to_dict() 方法，将这 3 个字段写入输出字典（仅在非空时写入）。
  </action>
  <verify>option_data.gd 包含 3 个新字段且 from_dict/to_dict 正确处理</verify>
  <acceptance_criteria>
    - option_data.gd 包含 "map_weights: Dictionary"
    - option_data.gd 包含 "force_map_id: String"
    - option_data.gd 包含 "battle_modifiers: Array[Dictionary]"
    - from_dict() 读取 map_weights, force_map_id, battle_modifiers
    - to_dict() 写入 map_weights, force_map_id, battle_modifiers
  </acceptance_criteria>
  <done>OptionData 新增地图权重、强制地图和战斗修改器字段</done>
</task>

<task type="auto">
  <name>Task 3: Update sample options.json with map_weights example</name>
  <files>data/options.json</files>
  <read_first>data/options.json</read_first>
  <action>
在 options.json 中找一个有 battle_trigger 的选项，添加示例 map_weights 和 battle_modifiers 字段。

具体：找到第一个包含 battle_trigger 的选项，添加：
```json
"map_weights": {"map_01": 0.7, "map_school": 0.3},
"battle_modifiers": [{"type": "add_waves", "value": 1}]
```

这仅作为数据格式示例，确保 OptionData.from_dict() 能正确解析。不需要添加 force_map_id 示例（大多数选项不需要强制地图）。
  </action>
  <verify>options.json 中至少一个选项包含 map_weights 字段</verify>
  <acceptance_criteria>
    - options.json 中至少一个选项包含 "map_weights" 键
    - options.json 中至少一个选项包含 "battle_modifiers" 键
  </acceptance_criteria>
  <done>options.json 包含 map_weights 和 battle_modifiers 示例数据</done>
</task>

</tasks>

<verification>
- [ ] Godot 编辑器无脚本错误
- [ ] GameSessionData 新字段可被其他脚本访问
- [ ] OptionData.from_dict() 正确解析新字段
- [ ] options.json 格式有效（JSON 语法正确）
</verification>

<success_criteria>
- 所有任务完成
- 数据模型扩展不影响现有功能
- 新字段有合理的默认值
</success_criteria>

<output>
After completion, create `.planning/phases/01-map-system/01-02-SUMMARY.md`
</output>

---
phase: 01-map-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/logic/tower_defense/map_config.gd
autonomous: true
requirements: [MAP-01, MAP-02, MAP-03]

must_haves:
  truths:
    - "MapConfig has era_id field for era filtering"
    - "MapConfig has difficulty parameter fields (wave_count_modifier, enemy_attribute_multiplier, tower_slot_count)"
    - "MapConfig auto-discovers maps by scanning res://resources/maps/ directory"
    - "Existing maps (map_01, map_test) still load correctly after refactoring"
  artifacts:
    - path: "scripts/logic/tower_defense/map_config.gd"
      provides: "Extended MapConfig with era_id, difficulty params, directory scanning"
      contains: "era_id"
  key_links:
    - from: "scripts/logic/tower_defense/map_config.gd"
      to: "res://resources/maps/"
      via: "DirAccess directory scan"
      pattern: "DirAccess\\.open.*resources/maps"
---

<objective>
扩展 MapConfig 数据模型，新增时代过滤和难度参数字段，并将硬编码注册表替换为目录扫描机制。

Purpose: 为后续的事件驱动地图选择和难度系统奠定数据基础。
Output: 增强的 MapConfig Resource 类，支持 era_id、难度参数和自动发现地图。
</objective>

<execution_context>
@E:/Test_MCP/first/TestMCP/.trae/get-shit-done/workflows/execute-plan.md
@E:/Test_MCP/first/TestMCP/.trae/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-map-system/01-CONTEXT.md
@.planning/phases/01-map-system/01-RESEARCH.md
@scripts/logic/tower_defense/map_config.gd
@resources/maps/map_01.tres
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add era_id and difficulty fields to MapConfig</name>
  <files>scripts/logic/tower_defense/map_config.gd</files>
  <read_first>scripts/logic/tower_defense/map_config.gd</read_first>
  <action>
在 MapConfig 类中新增以下 @export 字段（放在 max_towers 之后、camera 字段之前）：

1. `@export var era_id: String = ""` — 时代ID，空字符串表示通用地图，不受时代过滤限制
2. `@export_group("Difficulty")` — 新建导出分组
3. `@export var wave_count_modifier: int = 0` — 额外波次数（正数增加，负数减少）
4. `@export var enemy_attribute_multiplier: float = 1.0` — 敌人属性倍率（1.0=标准，>1=增强，<1=削弱）
5. `@export var tower_slot_count: int = -1` — 可用塔位数（-1=使用 max_towers 字段值，否则覆盖 max_towers）

不要修改任何现有字段或方法。
  </action>
  <verify>在 Godot 编辑器中打开 map_01.tres，确认新字段出现在 Inspector 中且默认值正确</verify>
  <acceptance_criteria>
    - map_config.gd 包含 "era_id: String"
    - map_config.gd 包含 "wave_count_modifier: int"
    - map_config.gd 包含 "enemy_attribute_multiplier: float"
    - map_config.gd 包含 "tower_slot_count: int"
    - map_config.gd 包含 "@export_group(\"Difficulty\")"
  </acceptance_criteria>
  <done>MapConfig 新增 era_id 和 3 个难度参数字段，默认值不影响现有地图行为</done>
</task>

<task type="auto">
  <name>Task 2: Replace hardcoded registry with directory scanning</name>
  <files>scripts/logic/tower_defense/map_config.gd</files>
  <read_first>scripts/logic/tower_defense/map_config.gd</read_first>
  <action>
替换 `_static_init()` 中的硬编码注册表为目录扫描逻辑：

1. 删除 `_static_init()` 中的硬编码字典
2. 新增静态方法 `_scan_maps_directory() -> Dictionary`：
   - 使用 `DirAccess.open("res://resources/maps/")` 扫描目录
   - 遍历所有 `.tres` 文件
   - 对每个文件用 `ResourceLoader.load()` 加载，验证是 MapConfig 类型
   - map_id 从文件名推导：`map_school.tres` → `map_school`（去掉扩展名）
   - 返回 `{map_id: path}` 字典
3. 修改 `_static_init()` 调用 `_scan_maps_directory()` 填充 `_map_registry`
4. 如果扫描失败（目录不存在等），fallback 到硬编码的 map_01 和 map_test
5. 保留 `register_map()` 方法不变
6. 保留 `get_map_ids()` 和 `load_map()` 方法不变

注意：扫描只在 _static_init 时执行一次，后续调用 load_map 使用缓存的注册表。
  </action>
  <verify>运行游戏，确认 map_01 和 map_test 仍可正常加载</verify>
  <acceptance_criteria>
    - map_config.gd 不再包含硬编码的 "map_01" 和 "map_test" 注册（仅在 fallback 中）
    - map_config.gd 包含 "DirAccess.open" 调用
    - map_config.gd 包含 "_scan_maps_directory" 方法
    - MapConfig.get_map_ids() 返回至少 ["map_01", "map_test"]
  </acceptance_criteria>
  <done>地图注册表通过目录扫描自动发现，现有地图仍可正常加载</done>
</task>

<task type="auto">
  <name>Task 3: Add helper method for effective tower slot count</name>
  <files>scripts/logic/tower_defense/map_config.gd</files>
  <read_first>scripts/logic/tower_defense/map_config.gd</read_first>
  <action>
在 MapConfig 中新增实例方法 `get_effective_tower_slots() -> int`：
- 如果 tower_slot_count == -1，返回 max_towers
- 否则返回 tower_slot_count

这个方法供 MapManager 使用，统一获取有效塔位数。
  </action>
  <verify>map_config.gd 包含 "get_effective_tower_slots" 方法</verify>
  <acceptance_criteria>
    - map_config.gd 包含 "func get_effective_tower_slots"
    - 方法正确处理 tower_slot_count == -1 的情况
  </acceptance_criteria>
  <done>MapConfig 提供 get_effective_tower_slots() 方法</done>
</task>

</tasks>

<verification>
- [ ] Godot 编辑器无脚本错误
- [ ] map_01.tres 和 map_test.tres 可正常加载
- [ ] MapConfig.get_map_ids() 返回至少 2 个地图
- [ ] 新字段在 Inspector 中可见且默认值正确
</verification>

<success_criteria>
- 所有任务完成
- 现有地图加载不受影响
- 新字段和目录扫描机制工作正常
</success_criteria>

<output>
After completion, create `.planning/phases/01-map-system/01-01-SUMMARY.md`
</output>

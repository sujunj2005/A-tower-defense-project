---
phase: 01-map-system
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - scripts/logic/life_simulation/event_system.gd
  - scripts/managers/map_manager.gd
autonomous: true
requirements: [MAP-01, MAP-03]

must_haves:
  truths:
    - "Event options accumulate map_weights into session when selected"
    - "Battle trigger selects map from accumulated weights with era filtering"
    - "Force_map_id overrides weight-based selection"
    - "MapManager reads current_map_id from session instead of hardcoded value"
    - "Accumulated weights are cleared after map selection"
  artifacts:
    - path: "scripts/logic/life_simulation/event_system.gd"
      provides: "Map weight accumulation and selection logic"
      contains: "accumulated_map_weights"
    - path: "scripts/managers/map_manager.gd"
      provides: "Dynamic map loading from session data"
      contains: "session.current_map_id"
  key_links:
    - from: "scripts/logic/life_simulation/event_system.gd"
      to: "scripts/bean/session_data.gd"
      via: "Writes accumulated_map_weights and current_map_id"
      pattern: "accumulated_map_weights"
    - from: "scripts/managers/map_manager.gd"
      to: "scripts/bean/session_data.gd"
      via: "Reads current_map_id for map loading"
      pattern: "session\\.current_map_id"
---

<objective>
实现事件驱动的地图选择机制：选项权重累加、战斗触发时选图、时代过滤、强制地图覆盖，以及 MapManager 从 session 动态读取地图ID。

Purpose: 让玩家的人生选择影响战斗地图，实现"选择上学→学校地图"的核心设计。
Output: EventSystem 处理地图权重，MapManager 动态加载选定地图。
</objective>

<execution_context>
@E:/Test_MCP/first/TestMCP/.trae/get-shit-done/workflows/execute-plan.md
@E:/Test_MCP/first/TestMCP/.trae/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-map-system/01-CONTEXT.md
@.planning/phases/01-map-system/01-RESEARCH.md
@.planning/phases/01-map-system/01-01-SUMMARY.md
@.planning/phases/01-map-system/01-02-SUMMARY.md
@scripts/logic/life_simulation/event_system.gd
@scripts/managers/map_manager.gd
@scripts/bean/session_data.gd
@scripts/bean/option_data.gd
@scripts/logic/tower_defense/map_config.gd
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add map weight accumulation in EventSystem.select_option()</name>
  <files>scripts/logic/life_simulation/event_system.gd</files>
  <read_first>scripts/logic/life_simulation/event_system.gd, scripts/bean/option_data.gd, scripts/bean/session_data.gd</read_first>
  <action>
在 EventSystem.select_option() 方法中，在处理 battle_trigger 之前（约第 127 行之前），添加地图权重累加和修改器收集逻辑：

```gdscript
# 累积地图权重
var opt_map_weights: Dictionary = option.get("map_weights", {})
if not opt_map_weights.is_empty():
    for map_id in opt_map_weights:
        var weight: float = float(opt_map_weights[map_id])
        session.accumulated_map_weights[map_id] = session.accumulated_map_weights.get(map_id, 0.0) + weight
    Global.debug_log("[事件系统] 累积地图权重: %s" % str(session.accumulated_map_weights))

# 收集战斗修改器
var opt_modifiers: Array = option.get("battle_modifiers", [])
if not opt_modifiers.is_empty():
    session.pending_battle_modifiers.append_array(opt_modifiers)
    Global.debug_log("[事件系统] 收集战斗修改器: %d个" % opt_modifiers.size())
```

注意：option 参数是 Dictionary 类型（从 events.json 解析），直接用 .get() 读取。OptionData 的 from_dict 已经处理了这些字段。
  </action>
  <verify>event_system.gd 包含 accumulated_map_weights 累加逻辑</verify>
  <acceptance_criteria>
    - event_system.gd 包含 "accumulated_map_weights" 引用
    - event_system.gd 包含 "pending_battle_modifiers" 引用
    - 权重累加在 battle_trigger 处理之前执行
  </acceptance_criteria>
  <done>EventSystem.select_option() 累积地图权重和收集战斗修改器</done>
</task>

<task type="auto">
  <name>Task 2: Add map selection logic in EventSystem._trigger_battle()</name>
  <files>scripts/logic/life_simulation/event_system.gd</files>
  <read_first>scripts/logic/life_simulation/event_system.gd, scripts/bean/session_data.gd, scripts/logic/tower_defense/map_config.gd</read_first>
  <action>
在 _trigger_battle() 方法中，在写入波次数据之前，添加地图选择逻辑：

```gdscript
# 地图选择
var selected_map_id: String = ""
var force_map: String = str(battle_trigger.get("force_map_id", ""))
if force_map != "":
    selected_map_id = force_map
    Global.debug_log("[事件系统] 强制地图: %s" % selected_map_id)
elif not session.accumulated_map_weights.is_empty():
    selected_map_id = _select_map_by_weights(session.accumulated_map_weights)
    session.accumulated_map_weights.clear()
    Global.debug_log("[事件系统] 权重选图: %s" % selected_map_id)
else:
    selected_map_id = "map_01"
    Global.debug_log("[事件系统] 默认地图: %s" % selected_map_id)

session.current_map_id = selected_map_id
```

新增私有方法 `_select_map_by_weights(weights: Dictionary) -> String`：
1. 获取当前 era_id：`session.era_id`
2. 获取所有可用地图ID：`MapConfig.get_map_ids()`
3. 过滤：仅保留 era_id 匹配的地图（地图 era_id 为空或等于当前 era_id）
4. 从 weights 中取过滤后的地图权重，选权重最高者
5. 如果所有权重为 0 或无匹配地图，返回 "map_01" 作为 fallback

注意：需要加载地图 Resource 来检查 era_id。为避免性能问题，可缓存已加载的 MapConfig。但首次实现时简单加载即可，地图数量有限。
  </action>
  <verify>event_system.gd 包含 _select_map_by_weights 方法</verify>
  <acceptance_criteria>
    - event_system.gd 包含 "_select_map_by_weights" 方法
    - event_system.gd 包含 force_map_id 处理逻辑
    - event_system.gd 在选图后清空 accumulated_map_weights
    - event_system.gd 将 selected_map_id 写入 session.current_map_id
  </acceptance_criteria>
  <done>战斗触发时根据权重和时代过滤选择地图</done>
</task>

<task type="auto">
  <name>Task 3: MapManager reads map_id from session</name>
  <files>scripts/managers/map_manager.gd</files>
  <read_first>scripts/managers/map_manager.gd, scripts/bean/session_data.gd</read_first>
  <action>
修改 MapManager._ready() 方法中的地图ID获取逻辑（约第 69-74 行）：

将：
```gdscript
func _ready() -> void:
    add_to_group("map_manager")
    if Global.debug_map_id != "":
        current_map_id = Global.debug_map_id
        Global.debug_map_id = ""
    _load_map_data(current_map_id)
```

改为：
```gdscript
func _ready() -> void:
    add_to_group("map_manager")
    if Global.debug_map_id != "":
        current_map_id = Global.debug_map_id
        Global.debug_map_id = ""
    else:
        var session: GameSessionData = Global.get_game_session()
        if session.current_map_id != "":
            current_map_id = session.current_map_id
    _load_map_data(current_map_id)
```

逻辑：debug_map_id 优先级最高（开发调试用），其次从 session 读取事件系统选定的地图，最后 fallback 到默认值 "map_01"。
  </action>
  <verify>map_manager.gd 包含 session.current_map_id 读取逻辑</verify>
  <acceptance_criteria>
    - map_manager.gd 包含 "session.current_map_id" 引用
    - debug_map_id 优先级高于 session.current_map_id
    - 默认值仍为 "map_01"
  </acceptance_criteria>
  <done>MapManager 从 session 动态读取地图ID</done>
</task>

</tasks>

<verification>
- [ ] Godot 编辑器无脚本错误
- [ ] 选择带 map_weights 的选项后，session.accumulated_map_weights 正确累加
- [ ] 战斗触发时，session.current_map_id 被正确设置
- [ ] MapManager 加载 session 指定的地图
- [ ] 无 map_weights 时 fallback 到 map_01
</verification>

<success_criteria>
- 所有任务完成
- 事件选项的地图权重正确累加
- 战斗触发时正确选图（含时代过滤）
- MapManager 动态加载地图
</success_criteria>

<output>
After completion, create `.planning/phases/01-map-system/01-03-SUMMARY.md`
</output>
